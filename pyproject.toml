[project]
name = "todoapp"
version = "0.1.0"
description = "Multi-list task management app"
requires-python = ">=3.13"
dependencies = [
    "django>=5.1",
    "markdown>=3.7",
    "bleach>=6.1",
]

[tool.black]
line-length = 88
