### Requirement: Graph filter checkboxes align inline with labels
Each filter checkbox in the graph sidebar Filters card SHALL render horizontally inline with its label text (checkbox on the left, label text to the right), not stacked vertically.

#### Scenario: Filter checkboxes display inline
- **WHEN** the graph page loads
- **THEN** each filter checkbox appears to the left of its label text on the same line
