## 1. Frontend: Default interaction date to today

- [x] 1.1 Add a `todayStr()` helper function that returns today's date as YYYY-MM-DD
- [x] 1.2 Initialize `newDate` state to `todayStr()` instead of empty string
- [x] 1.3 Reset `newDate` to `todayStr()` after successful form submission (instead of empty string)
