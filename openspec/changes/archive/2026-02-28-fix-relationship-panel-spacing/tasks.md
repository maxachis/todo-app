## 1. Fix panel grid alignment

- [x] 1.1 In `frontend/src/routes/relationships/+page.svelte`, add `align-content: start` to the `.panel` CSS rule to prevent grid rows from stretching to fill available height
- [x] 1.2 Verify the fix by checking that form inputs stay tightly spaced regardless of viewport height
