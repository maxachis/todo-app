## 1. Fix Label CSS

- [x] 1.1 In `frontend/src/routes/network/graph/+page.svelte`, update the `label` CSS rule to add `grid-template-columns: auto 1fr` and `align-items: center` so checkboxes render inline with their label text

## 2. Verify

- [x] 2.1 Visually confirm all filter checkboxes in the Filters card display inline with their labels
- [x] 2.2 Confirm other label elements on the graph page (range inputs, text inputs) still render correctly
