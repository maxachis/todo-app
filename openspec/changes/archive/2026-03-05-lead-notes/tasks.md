## 1. Frontend: Add notes to lead creation form

- [x] 1.1 Add `newNotes` state variable to the leads page component
- [x] 1.2 Add a `<textarea>` for notes in the create form (after the org TypeaheadSelect, before the submit button)
- [x] 1.3 Include `notes: newNotes` in the `api.leads.create()` payload
- [x] 1.4 Reset `newNotes = ''` after successful creation alongside other field resets
